# Core Framework (Pinned)
django==5.1.15
djangorestframework==3.17.2
django-cors-headers==4.9.0

# Database (PostgreSQL - Pinned)
psycopg==3.3.4
psycopg-binary==3.3.4
dj-database-url==3.1.2

# Async & Realtime (Pinned)
channels==4.3.2
channels-redis==4.3.0
daphne==4.2.3

# Tasks & Caching (Pinned)
celery==5.6.3
redis==8.1.0

# Configuration & Validation (Pinned)
python-dotenv==1.2.2
pydantic==2.13.4

# HTTP & SDK Client (Pinned)
requests==2.34.2
httpx==0.28.1

# OpenHands Integration Boundary (Exact Pinned Versions)
openhands-sdk==1.42.1; python_version >= "3.12"
openhands-tools==1.42.1; python_version >= "3.12"

# Testing & Dev (Pinned)
pytest==9.1.1
pytest-django==4.14.0
pytest-asyncio==1.4.0
